\section{Overview}

To further motivate our approach, consider the simplified two-phase commit (2PC) example introduced in \autoref{sec:intro}.
% \begin{enumerate} 
%     \item When the coordinator receives a $\eff{readReq}$ from the user, it forwards a $\eff{getReq}$ to the database. 
%     \item Upon receiving a $\eff{getRsp}$ from the database, the coordinator sends a $\eff{readRsp}$ back to the user with the same value ($va$ in the message payload). 
%     \item When the coordinator handles a $\eff{writeReq}$ from the user, it sends a $\eff{putReq}$ to the database with the specified write value ($va$ in the message payload). 
%     \item After receiving a $\eff{putRsp}$ from the database, the coordinator replies with a $\eff{writeRsp}$ to the user, using the same write value. Additionally, based on whether the $\eff{putRsp}$ status ($st$) is $\true$, the coordinator sends either a $\eff{commit}$ or $\eff{abort}$ message to the database to finalize or cancel the data modification. 
% \end{enumerate}
As shown in \autoref{fig:2pc-workflow}, from the user’s perspective (or more broadly, the environment’s), sending $\eff{readReq}$ and $\eff{writeReq}$ messages (marked in orange) does not depend on any prior messages. We refer to such messages as directly \emph{generatable}. In contrast, the remaining 8 messages are \emph{observable}—they are generated by the 2PC server and can only be observed by the user or environment. In this setting, the server (comprising the coordinator and database components) cannot operate independently. It requires the environment to provide generatable messages and control when the sent messages are received, through appropriate scheduling.



\paragraph{Trace and reachability property} The execution of a reactive system can produce a sequence of messages with concrete values as payloads, which we refer to as a \emph{trace}. One trace that is consistent with the plan $P_1$ in \autoref{sec:intro} is as follows:
{\small\begin{align*}
tr_1 \doteq 
 &\effGreen{writeReq(3)};\effGreen{putReq(3)};\effGreen{putRsp(3,\true)};\effGreen{writeRsp(3,\true)};
\\&\effBlue{readReq};\effBlue{getReq};\effBlue{getRsp(-1)};\effBlue{readRsp(-1)}; \effGreen{commit(3)}
\end{align*}}\noindent
As discussed earlier, this trace violates the strong consistency property because the read value $-1$ is the default value assumed by the database, which is not equal to the last successful write value ($3$). This trace is not unique in violating strong consistency; rather, it belongs to a family of traces that are parameterized by some read value $\Code{y}$ and the last successful write value $\Code{x}$. These traces can be described by the following \emph{reachability property} $\negSCA$ in symbolic Linear Temporal Logic on finite sequences (s-LTL$_f$):
{\footnotesize\begin{align*}
   \negSCA \doteq \finalA (\msg{writeRsp}{va = \Code{x} \land stat = \true} \land 
   (\neg \msg{writeRsp}{stat = \true}) \untilA \msg{readRsp}{va = \Code{y} \land \Code{y} \not= \Code{x}})
\end{align*}}\noindent

Here, $\msg{writeRsp}{va = \Code{x} \land stat = \true}$ represents an atomic predicate for a message, referred to as a \emph{symbolic event}. This indicates that the message is a $\eff{writeRsp}$ with $\Code{x}$ as the write value (i.e., field $va$) and a $\true$ status (i.e., field $st$) as its payload. We use the standard LTL final modality $\finalA$ and the until modality $\untilA$. The formula means: "there will \emph{eventually} be a successful $\eff{writeRsp}$ message with the write value $\Code{x}$, and moreover, there will be no further successful $\eff{writeRsp}$ message (i.e., $\Code{x}$ is the last write value) after it \emph{until} an $\eff{readRsp}$ message with a payload $\Code{y}$ different from $\Code{x}$ occurs."
The s-LTL$_f$ formula can be transformed into Symbolic Finite Automata (SFA), which provide a decidable procedure for inclusion and emptiness checks. It is important to note that the reachability property is actually an overapproximation of the desirable trace. Thus, not all traces consistent with the reachability property are necessarily realizable. For example, although the trace $\effGreen{writeRsp(3,\true)};\effBlue{readRsp(-1)}$ is the shortest trace consistent with $\negSCA$, it cannot be generated by the 2PC server because there is no corresponding request message in the trace.

\begin{figure}
    \centering
\begin{minted}[xleftmargin=5pt, numbersep=4pt, linenos = true, fontsize = \footnotesize, escapeinside=??]{OCaml}
?$\mykw{assert}$? (x != -1); 
?$\mykw{assert}$? (y == -1); 
?$\mykw{gen}$? ?$\eff{writeReq}$? x;
let (y1: int) = ?$\mykw{obs}$? ?$\eff{putReq}$?; ?$\mykw{assert}$? (y1 == x);
let (y2: int), (s1: bool) = ?$\mykw{obs}$? ?$\eff{putRsp}$?; ?$\mykw{assert}$? (y2 == x && s1 == true);
let (y3: int), (s2: bool) = ?$\mykw{obs}$? ?$\eff{writeRsp}$? in ?$\mykw{assert}$? (y3 == x && s2 == true); 
?$\mykw{gen}$? ?$\eff{readReq}$?;
?$\mykw{obs}$? ?$\eff{getReq}$?;
let (y4: int) = ?$\mykw{obs}$? ?$\eff{getRsp}$?; ?$\mykw{assert}$? (y4 == -1);
let (y5: int) = ?$\mykw{obs}$? ?$\eff{readRsp}$? in ?$\mykw{assert}$? (y5 == y && y != x); 
let (y6) = ?$\mykw{obs}$? ?$\eff{commit}$? in ?$\mykw{assert}$? (y6 == x); 
\end{minted}
    \caption{An plan program $e_{P_1}$ that is consistent with reachability property $\negSCA$. }
    \label{fig:2pc-term}
\end{figure}

\paragraph{Plan program} As mentioned earlier, components in reactive systems cannot operate independently without the support of the environment. We introduce a DSL for \emph{plan programs} that allow to express environments on specific message schedules instead of arbitrary message interleavings, as in a standard scheduler. The plan program manages the generation of missing messages, plans the message order, and constrains data dependencies between messages.
Concretely, to realize the trace $tr_1$, the environment must provide the generatable messages (e.g., $\effGreen{writeReq(3)}$ and $\effBlue{getReq}$) and ensure that the sent $\eff{commit}$ message is received after the other messages. Moreover, although $\Code{x}$ can be any value distinct from $\Code{y}$, $\Code{y}$ must be the default value $-1$, as assumed by the 2PC server implementation.
A plan program $e_{P_1}$ that implements the plan $P_1$ is shown in \autoref{fig:2pc-term}. The program's main body defines the \emph{receive} order of messages (lines 3-11) using the keywords $\mykw{gen}$ and $\mykw{obs}$, which indicate whether the message is directly generated by the environment. It is the environment's responsibility to provide the payload for generatable messages, and it can bind the payloads of observable messages to new variables. The constraints on parameters $\Code{x}$ and $\Code{y}$ are defined by the $\mykw{assert}$ statements on lines 1-2. 
Since the environment does not control the internal behavior of components, it cannot directly enforce specific traces. Instead, it can use assertions to guide the execution towards expected traces consistent with $\negSCA$. However, assertions may fail; for instance, if the database component sends a $\eff{putRsp}$ message with a $\false$ status, this would violate the assertion on line 5. Despite this, we can ensure that there \emph{exists} at least one possible execution of the reactive system that produces the expected traces in $\negSCA$.
To express multiple message orderings (e.g., both plans $e_{P_1}$ and $e_{P_2}$), our DSL allows the use of a non-deterministic choice operator, enabling the union of multiple plan programs.


% {\footnotesize\begin{align*}
%     &\Code{readReq}: \Code{unit} \quad& \Code{readRsp}: va\hasnty{int} \sarr \Code{unit}
%     \\&\Code{getReq}: \Code{unit} \quad&
%     \Code{getRsp}: va\hasnty{int} \sarr \Code{unit}
%     \\&\Code{writeReq}: va\hasnty{int} \sarr \Code{unit} \quad&
%     \Code{writeRsp}: va\hasnty{int} \sarr st\hasnty{bool} \sarr \Code{unit}
%     \\&\Code{putReq}: va\hasnty{int} \sarr \Code{unit} \quad&
%     \Code{putRsp}: va\hasnty{int} \sarr st\hasnty{bool} \sarr \Code{unit}
%     \\&\Code{commit}: va\hasnty{int} \sarr \Code{unit} \quad&
%     \Code{Abort}: \Code{unit}
% \end{align*}}

\subsection{Prophecy Automata Types}

While reachability properties are naturally expressed using s-LTL$_f$ and SFA, the behavior of a plan program cannot be directly captured by standard temporal logic due to the interaction between the environment and the components it orchestrates.
These components, such as the coordinator in the two-phase commit (2PC) example, can be seen as a set of message handlers (effectful functions). For example, the coordinator handles messages like $\eff{readReq}$, $\eff{writeReq}$, $\eff{getRsp}$, and $\eff{putRsp}$ (i.e., all input messages directed at the coordinator, as shown in \autoref{fig:2pc-workflow}). These handlers are stateful, meaning their current behavior is dependent on previously occurred events. 
Furthermore, due to the asynchronous nature of reactive systems, the result of one handler’s computation is not immediately observable. Instead, it materializes as a newly sent message, which may only be received at some future point. For example, the handler of a $\eff{getReq}$ message sends a $\eff{getRsp}$ message containing the read value as its payload. This read value is not arbitrary—it is the last value that was successfully $\eff{commit}$ed in the past. This dynamic nature, where message handlers both depend on past events and propagate their effects into the future, push us introduce the Prophecy Automata Type (PAT).

\begin{figure}
    \centering
{\footnotesize\begin{align*}
    \eff{readReq}:& \rg{\allTL}{\lastA\msg{readReq}{\top}}{\finalA \msg{getReq}{\top}}
    \\\eff{writeReq}:&  x\hasnty{int} \sarr \rg{\allTL}{\lastA\msg{writeReq}{va = x}}{\finalA\msg{putReq}{va = x}}
    \\\eff{getRsp}:& x\hasnty{int}\sarr \rg{\allTL}{\lastA\msg{getRsp}{va = x}}{\finalA\msg{readRsp}{va = x}}
    \\\eff{putRsp}:&  x\hasnty{int} \sarr s{:}\nuot{bool}{\vnu = \true} \sarr 
    \\&\quad \rg{\allTL}{\lastA\msg{putRsp}{va = x \land st = s}}{\finalA\msg{commit}{\top} \land \finalA \msg{writeRsp}{va = x \land st = s}}
    \\&  \interty x\hasnty{int} \sarr s{:}\nuot{bool}{\vnu = \false} \sarr 
    \\&\quad \rg{\allTL}{\lastA\msg{putRsp}{va = x \land st = s}}{\finalA\msg{Abort}{\top} \land \finalA \msg{writeRsp}{va = x \land st = s}}
    % \\\eff{readRsp}:& x\hasnty{int}\sarr \rg{\allTL}{\lastA\msg{readRsp}{va = x}}{\allTL}
    \\
    \\\eff{getReq}:&  x\hasnty{int}\garr \rg{\finalA \msg{commit}{va = x} \land \neg\nextA\finalA\msg{commit}{\top}}{\lastA\msg{getReq}{\top}}{\finalA\msg{readRsp}{ va = x}}
    \\&\interty  \rg{\neg\finalA \msg{commit}{\top}}{\lastA\msg{getReq}{\top}}{\finalA\msg{readRsp}{ \Code{va = -1}}}
    \\\eff{putReq}:&  x\hasnty{int} \sarr \rg{\allTL}{\lastA\msg{putReq}{va = x}}{\finalA\msg{putRsp}{va = x} }
    % \\\eff{writeRsp}:& x\hasnty{int} \sarr s\hasnty{bool} \sarr \rg{\allTL}{\lastA\msg{writeRsp}{va = x \land st = s}}{\allTL}
    \\\eff{commit}:& x\hasnty{int} \sarr \rg{\allTL}{\lastA\msg{commit}{va = x}}{\allTL}
    \\\eff{Abort}:& \rg{\allTL}{\lastA\msg{Abort}{\top}}{\allTL}
\end{align*}}
    \caption{2PC components Specification $\Delta$ that is expressed in Prophecy Automata Type (PAT) of messages }
    \label{fig:2pc-pat}
\end{figure}

PAT is inspired by Hoare Automata Type\cite{...} that a variant of refinement type that uses two Symbolic Finite Automata (SFA) to express pre- and post-condition effect contexts of a stateful program. Unlike HAT, PAT consists of three SFAs, where an additional ``prophecy automata'' is used to predict events that will occur in the future. Prophecy automata serve as the trace-based analogue of prophecy variables\cite{...}, which have been introduced in other state-based concurrency reasoning approaches.
The Prophecy Automata Type has the form $\rg{H}{A}{F}$, where $H$, $A$, and $F$ represent the history, present, and prophecy automata, respectively. For a plan program $e$ with PAT $\rg{H}{A}{F}$, it means that: ``If $e$ is executed with a history trace accepted by the history automaton $H$, then the execution of $e$ will produce a present trace accepted by the present automaton $A$, moreover, the future trace (the remaining part of the execution) will be accepted by the prophecy automaton $F$''.
This structure allows for a more nuanced reasoning about asynchronous message-passing systems, where both past and future events are connected with current events. The inclusion of prophecy automata adds flexibility in reasoning about future states in a way that traditional two-state specification (pre- and post-condition based) cannot, making it particularly suited to reactive systems.


\begin{example}[Component specification] The specification of the coordinator and database components in the 2PC example is shown in \autoref{fig:2pc-pat}, represented as two groups of message types. Messages are treated as effect operators and are denoted by function refinement types, where the automata in these types are still expressed in s-LTL$_f$. 
Since each message handling contributes exactly one event to the trace, the present automaton is always a singleton event, denoted by the standard $\lastA$ modality in s-LTL$_f$. The dependencies between messages are captured by the $\rg{H}{A}{F}$ triple.
For instance, the first type in \autoref{fig:2pc-pat} specifies that handling the $\eff{readReq}$ message results in sending a new message $\eff{getReq}$. In other words, a new event $\eff{getReq}$ will occur in the future ($\finalA \msg{getReq}{\top}$). The type of $\eff{getReq}$ consists of two cases, connected using an intersection type ($\interty$).
The first case indicates a $\eff{readRsp}$ message will eventually be triggered, with its payload equal to the last committed value (i.e., $\finalA \msg{commit}{va = x} \land \neg\nextA\finalA\msg{commit}{\top}$). This is expressed with the help of a ghost variable to link the last committed value in history automata into prophecy automata. The second case handles the situation where no committed value exists, then reads default value ($-1$).
\end{example}

\subsection{Plan Program Synthesis}

With the help of PAT, we can define all plans consistent with the semantics of the components as those plan programs whose message invocations ($\mykw{obs}$ and $\mykw{gen}$) are type-safe with respect to the message types provided in the component specification $\Delta$. It is easy to verify that the plan program $e_{P_1}$ satisfies these criteria. For instance, in the plan program $e_{P_1}$, the invocation of the message $\eff{getReq}$ on line 8 in \autoref{fig:2pc-term} divides the execution into three parts: the history (lines 1-7), the present (line 8), and the future (lines 9-11). This division aligns with the second case of the type for $\eff{getReq}$, since in the future trace, there is indeed a $\eff{getRsp}$ message with payload $-1$, and no $\eff{commit}$ message appears in the history.

On the other hand, the set of plan programs that are consistent with the reachability property $A$ can be defined as those programs that have the type $\rg{\epsilon}{A}{\epsilon}$. This type indicates that the plan program starts with an empty history trace and does not need to guarantee any further future events to be happened. 
Thus, a desirable plan program is one that resides in the "intersection" of programs consistent with both the component specification $\Delta$ and the reachability property $A$. 
Finding such a plan program can be formalized as a type-driven synthesis problem, i.e., search a plan program that has the type $\rg{\epsilon}{A}{\epsilon}$, where the message types are provided in the component specification $\Delta$. 
The naive component-based enumeration of these message invocation cannot solve this synthesis problem since it would suffer from exponential complexity.  Therefore, a more efficient synthesis algorithm should combine the specifications from both $\Delta$ and $A$ intelligently. 

\begin{figure}
    \centering
    \includegraphics[width=0.95\linewidth]{figures/intuition-plan-syn.drawio.png}
    \caption{Intuition of synthesis algorithm}
    \label{fig:intuition-algo}
\end{figure}

\paragraph{Synthesis algorithm} The intuition behind our synthesis algorithm is illustrated in \autoref{fig:intuition-algo}. As previously discussed, the essence of synthesizing a plan program lies in finding the "intersection" of behaviors permitted by both the component specifications $\Delta$ and the reachability property $A$. To operationalize this idea, our algorithm is structured into two main parts. The first part refines the property $A$ into a subset $A'$ (where $A' \subseteq A$) such that all message invocations in $A'$ are consistent with $\Delta$. The second part derives the corresponding plan program from $A'$.
The first part is the most crucial, as it needs to adapt PAT to constrain the local message invocations within property $A$ that is in a global view. To achieve this, we construct a refinement loop, as depicted in the loop structure in \autoref{fig:intuition-algo}. At each iteration of this loop, our algorithm selects a symbolic event $\msg{op}{\phi}$ from property $A$ to serve as a subgoal. It then partitions $A$ into three slices: $A_1$, $\msg{op}{\phi}$, and $A_2$.
Next, we choose a type $\tau$ from $\Delta$ that corresponds to the operator $\eff{op}$. The selected type $\tau$ may have the same message $\eff{op}$ in its present automata, allowing the algorithm to utilize its prophecy automata to refine the ``future'' (i.e., $A_2$) in a forward manner. Alternatively, the chosen type may feature the message $\eff{op}$ within its prophecy automata, which enables the algorithm to refine $A_1$ using its history and present automata in a backward manner. Regardless of the choice made, the merged result ($A'$) must contain at least one trace, which is guaranteed by the diamond box in \autoref{fig:intuition-algo} using standard SFA non-emptiness check. 
If the merged result $A'$ is not an empty automaton, the refinement loop continues; otherwise, we backtrack to select another type from $\Delta$ or switch to another subgoal. The selection of subgoals and types, as well as the decision to perform forward or backward synthesis, is guided by the searching heuristic embedded in our algorithm.
This loop terminates when all message invocations in $A$ have been verified to be consistent with $\Delta$. 

% \paragraph{Searching heuristic} There are two key intuitions:
% \begin{enumerate}
%     \item Focusing: if we doesn't finish one causal group of messages (e.g., messages for read operation that are colored in blue), we should not switch into another group of messages.
%     \item Backward to root: we always first use backward synthesis to find the generatable message that is the ``root'' of one operation, then perform forward synthesis.
% \end{enumerate}

\begin{example}[Synthesis] 
We will explain how our synthesis algorithm operates using our motivating example. For clarity, we represent both the property and PATs using desugared SFA in the form of Symbolic Regular Expressions (SRE). Initially, the desugared property $A$ is defined as follows:
{\footnotesize\begin{align*}
    A \doteq \anyA^* \seqA \msg{writeRsp}{va = \Code{x} \land stat = \true} \seqA (\anyA \setminus \msg{writeRsp}{stat = \true})^* \seqA \msg{readRsp}{va = \Code{y} \land \Code{y} \not= \Code{x}} \seqA \anyA^*
\end{align*}}\noindent
Assume we select the message $\eff{readRsp}$ from $A$ and choose to perform a backward synthesis, we can identify the corresponding type $\tau_\Code{getRsq}$ in $\Delta$ as following:
{\footnotesize\begin{align*}
    \eff{getRsp}:& x\hasnty{int}\sarr \rg{\allTL}{\lastA\msg{getRsp}{va = x}}{\finalA\msg{readRsp}{va = x}} \tag{$\tau_\Code{getRsq}$}
    \\& x\hasnty{int}\sarr \rg{\anyA^*}{\msg{getRsp}{va = x}}{\anyA^* \seqA \msg{readRsp}{va = x} \seqA \anyA^*} \tag{after desugar}
\end{align*}}\noindent
whose prophecy automata ($\anyA^* \seqA \msg{readRsp}{va = x} \seqA \anyA^*$) indicate that a read response is expected to occur in the future. After merge $A$ with $\tau_\Code{getRsq}$, the one possible merged result $A'$ will assume there is $\eff{getRsp}$ message with the same payload happens before $\eff{readRsp}$.
{\footnotesize\begin{align*}
    A' \doteq &\anyA^* \seqA \msg{writeRsp}{va = \Code{x} \land stat = \true} \seqA (\anyA \setminus \msg{writeRsp}{stat = \true})^* \seqA \textcolor{red}{\msg{getRsp}{va = x}} 
    \\& (\anyA \setminus \msg{writeRsp}{stat = \true})^* \seqA \msg{readRsp}{va = \Code{y} \land \Code{y} \not= \Code{x}} \seqA \anyA^*
\end{align*}}\noindent
Easily know $A'$ is not empty automata. Then, the $\eff{getRsp}$ is colored red to to signify that it has already been verified, indicating that the events before and after $\eff{getRsp}$ are already consistent with type $\tau_\Code{getRsq}$ from $\Delta$.
This process is termed backward synthesis, as the new event $\eff{getRsp}$ is introduced prior to $\eff{readRsp}$. 
The backward synthesis will continue until we face the generatable message. For example, $\eff{getRsp}$ is actually triggered by another event $\eff{getReq}$, and finally triggered by the generatable message $\eff{readReq}$. 

On the other hand, we can also preform forward synthesis at $\eff{readRsp}$, which involves directly merging $A$ with the type of $\eff{readRsp}$. Since the receiver $\eff{readRsp}$ is user (environment), thus it won't trigger any new message and omitted from \autoref{fig:2pc-pat}. Thus, it will have type
{\footnotesize\begin{align*}
    \eff{readRsp}:& x\hasnty{int}\sarr \rg{\allTL}{\lastA\msg{readRsp}{va = x}}{\allTL}
    \\& x\hasnty{int}\sarr \rg{\anyA^*}{\msg{readRsp}{va = x}}{\anyA^*} \tag{after desugar}
\end{align*}}\noindent
which induce a merged result $A''$ shown below.
{\footnotesize\begin{align*}
    A'' \doteq &\anyA^* \seqA \msg{writeRsp}{va = \Code{x} \land stat = \true} \seqA (\anyA \setminus \msg{writeRsp}{stat = \true})^* \seqA \textcolor{red}{\msg{getRsp}{va = x}} 
    \\& (\anyA \setminus \msg{writeRsp}{stat = \true})^* \seqA \textcolor{red}{\msg{readRsp}{va = \Code{y} \land \Code{y} \not= \Code{x}}} \seqA \anyA^*
\end{align*}}\noindent

It is important to note that the Kleene star in property $A$ can be straightforwardly underapproximated to $\epsilon$ automata. Consequently, the processes of backward and forward synthesis conclude once all symbolic events that are not encapsulated within the Kleene star have been resolved (indicated in red). As a result, one potential refinement of the automaton can be represented as follows:
{\footnotesize\begin{align*}
    &\msg{writeReq}{va = \Code{x}}\seqA \msg{putReq}{va = \Code{x}}\seqA \msg{putReq}{va = \Code{x} \land stat = \true}\seqA \msg{writeRsp}{va = \Code{x} \land stat = \true} \seqA 
    \\&\msg{readReq}{\top} \seqA \msg{getReq}{\top} \seqA \msg{getRsp}{va = \Code{y} \land \Code{y} = -1} \seqA \msg{readRsp}{va = \Code{y} \land \Code{y} \not= \Code{x}} \seqA \msg{readRsp}{va = \Code{y} \land \Code{y} \not= \Code{x}} \seqA
    \\&  \msg{commit}{va = \Code{x}}
\end{align*}}\noindent
which can derive a plan program $e_{P_1}$ shown in \autoref{fig:2pc-term}.
\end{example}

The details of algorithm will explained in \autoref{sec:algo}.