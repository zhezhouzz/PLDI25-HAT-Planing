Read an un-written value will lead status of $\eff{readRsp}$ to be false.

The specification is the same.

The erroneous trace:

{\small\begin{align*}
tr_1 \doteq 
&\effBlue{writeReq(4)};\effRed{readReq};\effRed{getReq};\effRed{getRsp(-1,\true)};\effBlue{putReq(4)};\effBlue{putRsp(4,\true)};\effBlue{writeRsp(4,\true)};
\\&\effRed{readRsp(-1,\true)}
\end{align*}}\noindent


The synthesized program:

\begin{figure}
    \centering
\begin{minted}[xleftmargin=5pt, numbersep=4pt, linenos = true, fontsize = \footnotesize, escapeinside=??]{OCaml}
?$\mykw{assert}$? (x != -1 && y == -1); 
?$\mykw{gen}$? ?$\eff{writeReq}$? x;
?$\mykw{gen}$? ?$\eff{readReq}$?;
let () = ?$\mykw{obs}$? ?$\eff{getReq}$?;
let (x2: int) (s1: bool) = ?$\mykw{obs}$? ?$\eff{getRsp}$?; ?$\mykw{assert}$? (y1 == -1 && s1 == true);
let (x1: int) = ?$\mykw{obs}$? ?$\eff{putReq}$?; ?$\mykw{assert}$? (x1 == x);
let (x2: int) (s3: bool) = ?$\mykw{obs}$? ?$\eff{putRsp}$?; ?$\mykw{assert}$? (x2 == x && s2 == true);
let (x2: int) (s3: bool) = ?$\mykw{obs}$? ?$\eff{writeRsp}$?; ?$\mykw{assert}$? (x2 == x && s3 == true);
let (y2: int) (s4: bool) = ?$\mykw{obs}$? ?$\eff{readRsp}$?; ?$\mykw{assert}$? (y2 == -1 && s4 == true)
\end{minted}
    \caption{An plan program $e_{P_1}$ that is consistent with reachability property $\negSCA$. }
    \label{fig:2pc-term}
\end{figure}

Another possible trace:

{\small\begin{align*}
tr_2 \doteq 
&\effBlue{writeReq(4)};\effRed{readReq};\effRed{getReq};\effBlue{putReq(4)};\effBlue{putRsp(4,\true)};\effBlue{writeRsp(4,\true)};
\\&\effRed{getRsp(-1,\true)};\effRed{readRsp(-1,\true)}
\end{align*}}\noindent

\begin{figure}
    \centering
{\footnotesize\begin{align*}
    \eff{readReq}:& \rg{\allTL}{\lastA\msg{readReq}{\top}}{\finalA \msg{getReq}{\top}}
    \\\eff{getReq}:&  \Code{x}\hasnty{int}\garr \rg{\finalA \msg{putRsp}{v = \Code{x} \land st} \land \neg\nextA\finalA\msg{putRsp}{st}}{\lastA\msg{getReq}{\top}}{\finalA\msg{getRsp}{ v = \Code{x}}}
    \\&\interty  \rg{\neg\finalA \msg{putRsp}{\top}}{\lastA\msg{getReq}{\top}}{\finalA\msg{getRsp}{ v = \Code{-1}}}
    \\\eff{getRsp}:& \Code{x}\hasnty{int}\sarr \rg{\allTL}{\lastA\msg{getRsp}{v = \Code{x}}}{\finalA\msg{readRsp}{v = \Code{x}}}
   \\\eff{readRsp}:& \Code{x}\hasnty{int} \sarr \Code{s}\hasnty{bool} \sarr \rg{\allTL}{\lastA\msg{readRsp}{v = \Code{x} \land st = \Code{s}}}{\allTL}
    % \\\eff{readRsp}:& \Code{x}\hasnty{int}\sarr \rg{\allTL}{\lastA\msg{readRsp}{v = \Code{x}}}{\allTL}
    \\
    \\\eff{writeReq}:&  \Code{x}\hasnty{int} \sarr \rg{\allTL}{\lastA\msg{writeReq}{v = \Code{x}}}{\finalA\msg{putReq}{v = \Code{x}}}
    \\\eff{putReq}:&  \Code{x}\hasnty{int} \sarr \rg{\allTL}{\lastA\msg{putReq}{v = \Code{x}}}{\finalA\msg{putRsp}{v = \Code{x}} }
     \\\eff{putRsp}:&  \Code{x}\hasnty{int} \sarr \Code{s}\hasnty{bool} \sarr \rg{\allTL}{\lastA\msg{putRsp}{v = \Code{x} \land st = \Code{s}}}{\finalA \msg{writeRsp}{v = \Code{x} \land st = \Code{s}}}
    \\\eff{writeRsp}:& \Code{x}\hasnty{int} \sarr \Code{s}\hasnty{bool} \sarr \rg{\allTL}{\lastA\msg{writeRsp}{v = \Code{x} \land st = \Code{s}}}{\allTL}
\end{align*}}
    \caption{2PC components Specification $\Delta$ that is expressed in Prophecy Automata Type (PAT) of messages }
    \label{fig:2pc-pat}
\end{figure}

% \begin{figure}
%     \centering
% {\footnotesize\begin{align*}
%     \eff{writeReq}:&  \Code{x}\hasnty{int} \sarr \rg{\allTL}{\lastA\msg{writeReq}{v = \Code{x}}}{\finalA\msg{writeRsp}{v = \Code{x}}}
%     \\\eff{writeRsp}:& \Code{x}\hasnty{int}\sarr \rg{\allTL}{\lastA\msg{writeRsp}{v = \Code{x} }}{\allTL}
%     \\
%     \\\eff{readReq}:&  \Code{x}\hasnty{int}\garr \rg{\finalA \msg{writeReq}{v = \Code{x}} \land \neg\nextA\finalA\msg{writeReq}{\top}}{\lastA\msg{readRsp}{\top}}{\finalA\msg{readRsp}{v = \Code{x} \land st}}
%     \\&\interty  \rg{\neg\finalA \msg{writeReq}{\top}}{\lastA\msg{readReq}{\top}}{\finalA\msg{readRsp}{ \neg st}}
%     \\\eff{readRsp}:& \Code{x}\hasnty{int}\sarr \Code{s}\hasnty{bool} \sarr \rg{\allTL}{\lastA\msg{readRsp}{v = \Code{x} \land st = \Code{s}}}{\allTL}
% \end{align*}}
%     \caption{2PC components Specification $\Delta$ that is expressed in Prophecy Automata Type (PAT) of messages }
%     \label{fig:2pc-pat}
% \end{figure}

The interesting type of $\eff{readReq}$, which is a intersection type. the First case is similar with the cased introduced in the Sec.1. The second type means if there is no success write before, it will return the default value $-1$. Note that the status of $\eff{readRsp}$ has no constraint, thus the database can always reject the request.

{\footnotesize\begin{align*}
    &\eff{readReq}: \Code{x}\hasnty{int}\sarr \rg{\anyA^* \seqA \msg{putRsp}{v = \Code{x} \land st } \seqA (\anyA \setminus \msg{putRsp}{st})^*}{\msg{readReq}{\top}}{ \anyA^* \seqA \msg{readRsp}{ v = \Code{x} } \seqA \anyA^*  }
    \tag{after desugar}
    \\&\eff{readReq}: \rg{(\anyA \setminus \msg{putRsp}{st})^*}{\msg{readReq}{\top}}{ \anyA^* \seqA \msg{readRsp}{v = -1} \seqA \anyA^*  }
    \tag{after desugar}
\end{align*}}\noindent