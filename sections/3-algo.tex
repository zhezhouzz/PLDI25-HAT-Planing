\section{Synthesis}
\label{sec:synthesis}

When typing a program using our declarative typing rules, we can
freely apply the subsumption rule to align the (ordered) set of events
performed by the program with a \PAT{} that describes a user's desired
high-level property. Any \emph{synthesis} procedure based on such a
high-level specification, must devise a similar ordering alongside the
events in the program it generates. At the same time, each event needs
to align with the specification of its handler in $\Delta$, i.e., its
temporal and data-dependency constraints must be satisfied. Our
solution to this problem is a refinement loop, depicted in
\autoref{fig:intuition-algo}, that iteratively refines the high-level
specification into one that is consistent with these constraints. Each
iteration of this loop targets a single event, adding events before
and after that message so that its dependences are satisfied, i.e., so
that the corresponding handler at that point in the synthesized
program is well-typed. While declarative typing rules always assume
\PAT{}s are well-formed, our loop employs an automata non-emptiness
check to ensure it represents a controller that produces at least one
feasible trace. After the refinement loop has finished, a
corresponding well-typed controller program can be mechanically
extracted from the refined property.

% To convert the declarative typing rules given in
% \autoref{fig:selected-typing-rules} into an efficient synthesis
% algorithm, we must address a critical issue, i.e., finding a refined
% violation property (e.g., $\negSCA'$) from the original (e.g.,
% \ref{eqn:autoEx}).  Recall that this refined property has a more sequential
% form that indicates a specific message order that the synthesized
% controller is obligated to follow. Moreover, as explained in
% \autoref{sec:overview}, every symbolic event in $\negSCA'$ needs to
% align with the handler specification $\Delta$, i.e., temporal and
% data-dependency constraints found in $\Delta$ must be manifest in
% $\negSCA'$.

% The declarative subsumption rule \textsc{TSub} serves as an oracle to
% allow us to transform a violation property into the correct shape, but
% an efficient algorithm needs to achieve this iteratively.

% \textsc{TSub} also makes sure that ghost
% variables and parameters types of function \PAT are suitably
% strengthened before they get added into the typing context

\subsection{Abstract trace}

Our algorithm targets automata that have been normalized into an
\emph{abstract trace}, a sequence of singleton events
$\lastA \msg{op}{\phi}$. This normal form makes it easy to identify
the traces that must precede and follow each event $\msg{op}{\phi}$ in
an SFA's traces.

\begin{definition}[Abstract Trace] An abstract trace $\Pi$ is an SFA,
  encoded by a symbolic LTL$_f$ formula defined by the following
  grammar: %
  {\small
    \begin{alignat*}{2}
      \text{\textbf{Abstract Trace}}& \quad &\Pi  ::= \quad & \lastA \msgB{op}{\overline{x}}{\phi} ~|~ \globalA A ~|~ \Pi \seqA \Pi
    \end{alignat*}} %
  \noindent
  Every symbolic LTL$_f$ formula can be normalized into a finite set
  of abstract traces.
\end{definition}

\begin{example}[Abstract trace] The formula encoding violations of a
  Read Your Writes policy, \ref{eqn:autoEx}, can be normalized into the
  following abstract trace: %
  {\footnotesize
    \begin{align*}
      \lastA \msg{writeRsp}{v =  \Code{x}} \seqA \globalA (\neg
        \msg{writeRsp}{\top}) \seqA  \lastA \msg{readRsp}{v =
        \Code{y} \land st = \true \land \Code{y} \not= \Code{x}} \seqA
      \allTL
      \label{eqn:abstraceEx}\tag{$\negSCAPi$}
    \end{align*}}\noindent
  This formula captures the executions of our database example in
  which a successful $\eff{readRsp}$ message carries a value different
  from the last observed $\eff{writeRsp}$ message.
\end{example}

\subsection{Synthesis Algorithm}

\setlength{\textfloatsep}{4pt}
\begin{algorithm}[t!]
  \Params{Ghost variables $\overline{x{:}b}$, handler context
    $\Delta$, and abstract unsafe trace $\Pi$}%
  \Output{Controller $e$, such that
    $\Gamma; \Delta; \emptyset \vdash e~:~\rg{\epsTL}{\Pi}{\epsTL}$} %
  $\Mfw, \Mbw \leftarrow \emptyset, \emptyset$ \tcp*[l]{initialize forward and backward set.}
  $\Gamma \leftarrow \overline{x{:}\rawnuot{b}{\top}}$
  \tcp*[l]{initialize type context}
% $\Gsender \leftarrow \emptyset$ \tcp*[l]{A global message buffer}
% $\Gchecked \leftarrow \emptyset$ \tcp*[l]{ }
\tcp{Pick a previously unexamined symbolic event $\msg{op}{\phi}$}
\While{exists $\msgB{op}{\overline{x}}{\phi}$ s.t. $\Pi = \Pi_h \seqA \lastA \msgB{op}{\overline{x}}{\phi} \seqA \Pi_f$ and  $\msgB{op}{\overline{x}}{\phi} \not\in (\Mfw \cap \Mbw)$}{
    % \tcp*[l]{add qualifier of $\eff{op}$ event into type context, $z$ fresh}
    % $\Gamma \leftarrow \Gamma, \overline{y\,{:}\rawnuot{b}{\top}},\, z\,\hasoty{unit}{\phi[x\mapsto y]}$\;
    \If{$\eff{op} \not\in \Mfw$}{
        $(\Gamma, \Mfw, \Mbw, \Pi_h, \lastA \msgA{op}{\overline{y}}, \Pi_f) \leftarrow \forward(\Delta, \Gamma, \Mfw, \Mbw, \Pi_h, \lastA
        \msgA{op}{\overline{y}}, \Pi_f)$\;
    }
    \If{$\eff{op} \notin \Mbw$}{
        \lIf{$\GEN(\eff{op})$}{
            $\Mbw \leftarrow \Mbw \cup \{\eff{op}\}$
        }
        \lElse {
        $(\Gamma, \Mfw, \Mbw, \Pi_h, \lastA \msgA{op}{\overline{y}}, \Pi_f) \leftarrow \backward(\Delta,\Gamma,\Mfw, \Mbw, \Pi_h, \lastA \msgA{op}{\overline{y}}, \Pi_f)$
        }
    }
    % $\Gchecked \leftarrow \Gchecked \cup \{\eff{op}\}$ \tcp*[l]{now message $\eff{op}$ is check}
    % $\Gsender \leftarrow \Gsender \setminus \Gchecked$ \tcp*[l]{removed checked message from buffer}
    $\Pi \leftarrow \Pi_h \seqA \lastA \msgA{op}{\overline{y}} \seqA
    \Pi_f$ \tcp*[l]{refine unsafe abstract trace}
}
$e \leftarrow \S{DeriveTerm}(\Gamma, \Pi)$ \tcp*[l]{derive controller program}
\Return{$e$}\;
\caption{Synthesis}
\label{algo:top-syn}
\end{algorithm}

Our top-level synthesis algorithm is shown in \autoref{algo:top-syn}.
Given an (unsafe) abstract trace $\Pi$ and corresponding ghost variables (e.g., $\Code{x}$ and $\Code{y}$ in \ref{eqn:abstraceEx}) as input, this nondeterministic
algorithm synthesizes a well-typed $\DSL{}$ controller. The algorithm
follows the structure given in \autoref{fig:intuition-algo}, using a
refinement loop (lines $3$ - $9$) to refine the input abstract trace
$\Pi$ and then deriving the final controller from the refined property
(line $10$). Each iteration of this loop nondeterministically chooses
a target event that is used to refine the current abstract trace;
different choices may result in different message orders, and some of
these choices may cause the algorithm to fail.  Our implementation
resolves this nondeterminism in the algorithm via an efficient
backtracking search procedure that takes the union of all successful
runs in order to capture different orderings.

% \paragraph{Type context} In order to derive necessary
% assumption and assertions over variables in the synthesized program,
% we need to gather qualifiers distributed among different symbolic
% events in the violation property into the type context $\Gamma$, which
% is also refined by the refinement loop.  For example,
% line $4$ in \autoref{algo:top-syn} lifts the local
% variables $\overline{x}$ and qualifier $\phi$ of a symbolic event
% $\msgB{op}{\overline{x}}{\phi}$, into $\Gamma$
% $\overline{y{:}\rawnuot{b}{\top}},\
% x\,{:}\ \nuot{unit}{\phi[\overline{x\mapsto y}]} \vdash
% \msgB{op}{\overline{z}}{\overline{x = y}}$, where $z$ is a fresh variable. We use syntax sugar
% $\msgA{op}{\overline{y}}$ to stand for
% $\msgB{op}{\overline{x}}{\overline{x = y}}$ in the rest of paper.

\paragraph{Event dependencies} The refined abstract trace produced by
our loop must correspond to a well-typed program, i.e., the traces
preceding and following each of its events must be consistent with the
specifications of its corresponding handler. The events that will
precede and follow each event is not known until the loop is finished,
so we cannot simply track the set of observable events, as the
declarative typing rules did via $\Theta$. Instead, each iteration of
the loop detects the unresolved dependencies of a target event in the
current abstract trace and repairs them by inserting appropriate
events before and after it. Intuitively, each observable event must
$\eff{op_{child}}$ must follow a handler of an operation that produces
it, $\eff{op_{parent}}$, forming a tree-like dependency structure
similar to that described in \cite{MessageChain}. The refinement loop
reconstructs these trees from the target node, refining the candidate
abstract trace into one that satisfies these dependencies.

\begin{example}[Message dependency] The refined unsafe abstract trace
  \ref{eqn:autoEx2} contains three distinct sets of messages,
  comprised of pairs of requests and corresponding responses: %
  {\footnotesize
    \begin{align*}
      &\lastA \msg{writeReq}{v = \Code{x}} \seqA
        \lastA \msg{writeReq}{v =\Code{y} \land v \not=\Code{x}} \seqA \lastA \msg{readReq}{\top} \seqA \\
      &\lastA \msg{writeRsp}{v = \Code{y}} \seqA
        \lastA \msg{writeRsp}{v = \Code{x}} \seqA
        \lastA \msg{readRsp}{v = \Code{y} \land st = \true \land v \not=
        \Code{x}}
    \end{align*}
  }\noindent In contrast, the original trace \ref{eqn:abstraceEx} only
  contains the last two events in \ref{eqn:autoEx2}.
\end{example}

\paragraph{Trace refinement loop} Resolving the dependencies of a
target event requires us to insert events that must precede it and
events that must follow it; the former constitutes the \emph{backward}
synthesis phase of the algorithm, while the latter is subsumed by a
\emph{forward} synthesis pass. The two phases are symmetric: if the
prophecy automaton of the message handler for the operation
$\eff{op_{parent}}$ includes the operation $\eff{op_{child}}$,
performing forward synthesis on $\eff{op_{parent}}$ is the same as
performing backward synthesis on $\eff{op}$. To avoid repeatedly
targeting the same event, our algorithm maintains two sets $\Mfw$ and
$\Mbw$ that record events whose child and parent dependencies have
already been resolved, resp.\footnote{
To enable $\Mfw$ and $\Mbw$ to distinguish distinct occurrences of events with the same effect operator in the abstract trace, we assign a unique identifier to each operator at every occurrence. For example, \ref{eqn:abstraceEx} with identifiers can be $\lastA \msg{writeReq_1}{v = \Code{x}} \seqA \lastA \msg{writeReq_2}{v =\Code{y}}...$. } The intersection of these
two sets ($\Mfw \cap \Mbw$) contains those events that correspond to
well-typed handlers in a controller program.

$\Mfw$ and $\Mbw$ are empty (line $1$) when the refinement loop (lines
3-9 of \autoref{algo:top-syn}) starts, and the type context consists
of ghost variables whose qualifiers are $\top$ (line $2$). During each
iteration, a symbolic event in the current abstract trace $\Pi$ is
selected ($\msg{op}{\phi} \not\in \Mfw \cap \Mbw$ on line $3$); the
algorithm terminates once the dependencies of all symbolic events in
$\Pi$ are resolved. $\Pi$ is partitioned into the history and future
traces, $\Pi_f$ and $\Pi_f$, that surround the target event. The
algorithm tries to perform forward (resp., backward) synthesis on
these traces, if the event is not in $\Mfw$ (resp., $\Mbw$).  If the
target operation is generable ($\GEN(\eff{op})$ on line $7$), it is
the root of a dependency chain, so no additional backward synthesis is
required and the event is simply added to $\Mbw$ (line $8$). Both the
forward and backward synthesis routines yield a 6-tuple
$(\Gamma, \Mfw, \Mbw, \Pi_h, \lastA \msgA{op}{\overline{x}}, \Pi_f)$
that contains updated history and future traces; the refined abstract
trace at the end of an iteration (line $9$) is simply the
concatenation of the refined history trace, target event, and refined
future trace.

\setlength{\textfloatsep}{4pt}
\begin{algorithm}[t!]
% \Params{A 5-tuple $(\Gamma, \Pi_h, \lastA \msgA{op}{\overline{x}}, \Pi_f, \Gchecked)$ that are type context, history, current, and future automata, as well as checked message set.}
% \Output{Set of refined 5-tuples }
\Procedure{$\forward(\Delta, \Gamma, \Mfw, \Mbw,\Pi_h, \lastA \msg{op}{\phi}, \Pi_f)$}{
  \tcp{Select the \PAT of $\eff{op}$ from handler specification}
    % \SJ{I don't understand this form - the braces at the end are not part of a PAT}
    \If{$\Delta(\eff{op}) = \langle \overline{z{:}b}\garr\overline{y{:}t}\sarr\rg{H}{\lastA\msg{op}{\phi'}}{F}, B \rangle \in \Delta$}{
        $\Gamma \leftarrow \Gamma, \overline{z{:}\rawnuot{b}{\top}}, \overline{y{:}t}$ \tcp*[l]{add ghost variables and parameters types in type context}
      % \tcp{add qualifier of $\eff{op}$ event into type context}
      % \SJ{Didn't we already do this at line 4 in Algorithm 1?}
        $\msg{op}{\phi} \leftarrow \msg{op}{\phi \land \phi'}$ \tcp*[l]{merge current automata}
        $\Pi_h \leftarrow \Pi_h \land H$ \tcp*[l]{merge history automata}
        $\Pi_f \leftarrow \Pi_f \land F$ \tcp*[l]{merge prophecy automata}
        \tcp{non-emptiness check}
        \If{$\Gamma \not\vdash (\Pi_h \seqA \lastA\msg{op}{\phi} \seqA \Pi_f) \subseteq \neg\allTL$}{
            \tcp{return type context, property, as well as updated forward and backward set}
            \Return{$(\Gamma', \Mfw \cup \{\eff{op}\}, \Mbw \cup B, \Pi_h, \lastA \msg{op}{\phi}, \Pi_f)$}
        }
    }
}
\caption{Forward Synthesis}
\label{algo:forward}
\end{algorithm}

\paragraph{Forward and backward synthesis} The forward synthesis
subroutine is shown in \autoref{algo:forward}. It first retrieves the
\PAT{} of the target operation $\eff{op}$ from $\Delta$ (line $2$); it
also uses $\Delta$ to retrieve any children (future) dependencies
events of $\eff{op}$.  The algorithm then merges the selected \PAT
into the violation property piecewise. First, the occurence of the
target operation in the current abstract trace $\eff{op}$ is aligned
with its specification in $\Delta$ (line $4$). Next, the algorithm
merges the history and future traces with the \PAT{}'s history and
future automata (lines $5-6$). In order to guarantee the refined
abstract trace contains at least one realizable trace, the algorithm
checks for non-emptiness of the violation property (line $7$) by
ensuring the refined automata,
$\Pi_h \seqA \lastA\msgA{op}{\overline{x}} \seqA \Pi_f$, is not empty,
similar to \textsc{WfHAF} .
% Observe
% that under different substitutions, $\sigma \in \denot{\Gamma}$, the
% violation property may have have different denotations. We would like
% to find a refined type context $\Gamma'$ such that the interpretation
% of the property under this context is non-empty.  We do so   on
% line $6$, where $\Gamma' \sqsubseteq \Gamma$ is defined as:
% {\small\begin{align*} \Gamma' \sqsubseteq \Gamma \doteq
%   \denot{\Gamma'} \subseteq \denot{\Gamma} \land \denot{\Gamma'}
%   \not=\emptyset
% \end{align*}}\noindent
% \SJ{Would like a tighter phrasing of this point that doesn't
%   involve introducing abduction}
% Here, we apply a filtering procedure to discard infeasible
% substitutions~\cite{ZYDJ24}
Finally, the algorithm returns the refined type context, property, as
well as updated forward and backward sets (line $8$).

% \SJ{Stopped here ...}

\setlength{\textfloatsep}{4pt}
\begin{algorithm}[t!]
\Procedure{$\backward(\Delta, \Gamma, \Mfw, \Mbw, \Pi_h, \lastA \msg{op}{\phi}, \Pi_f, )$}{
    \tcp{Select the \PAT of some $\eff{op_{parent}}$ from the handler specification, who sends a message with operator $\eff{op}$}
    \If{$\Delta(\eff{op_{parent}}) = \langle \overline{z{:}b}\garr\overline{y{:}t}\sarr\rg{H}{\msg{op_{parent}}{\phi_\Code{parent}}}{F_1 \seqA \lastA \msg{op}{\phi'} \seqA F_2}, \{\eff{op}\} \cup \Theta\rangle \in \Delta$}{
        % \tcp{add qualifier of $\eff{op_{parent}}$ and $\eff{op}$ event into type context}
        $\Gamma \leftarrow \Gamma, \overline{z{:}\rawnuot{b}{\top}}, \overline{y{:}t}$ \tcp*[l]{add ghost variables and parameters types into the type context}
        $\msg{op}{\phi} \leftarrow \msg{op}{\phi \land \phi'}$ \tcp*[l]{merge current automata}
        $\Pi_h \leftarrow \Pi_h \land (H \seqA \lastA \msg{op_{parent}}{\phi_\Code{parent}} \seqA F_1)$ \tcp*[l]{merge history automata}
        $\Pi_f \leftarrow \Pi_f \land F_2$ \tcp*[l]{merge prophecy automata}
        \tcp{non-emptiness check}
        \If{$\Gamma \not\vdash (\Pi_h \seqA \lastA\msg{op}{\phi} \seqA \Pi_f) \subseteq \neg\allTL$}{
            \tcp{return type context, property, as well as updated forward and backward operations set}
            \Return{$(\Gamma', \Mfw \cup \{\eff{op_{parent}}\}, \Mbw \cup \{\eff{op}\} \cup \Theta, \Pi_h, \lastA \msg{op}{\phi}, \Pi_f)$}
        }
    }
}
\caption{Backward Synthesis}
\label{algo:backward}
\end{algorithm}

The backward synthesis subroutine, shown in \autoref{algo:backward},
is similar to the forward synthesis procedure works backward from a
target event, insert preceding events into $\Pi_h$ to resolve parent
dependencies. The change in direction results to several differences
with its forward counterpart. The procedure now selects a parent
operator $\eff{op_{parent}}$ whose handler specification has a
prophecy automata that includes the target operator $\eff{op}$.  The
refined abstract trace needs to align the target operator $\eff{op}$
with its counterpart in the prophecy automata of $\eff{op_{parent}}$:
{\footnotesize
  \begin{align*} \underbrace{ \effL H \effR \effL
    \lastA\msg{op_{parent}}{\phi_\Code{parent}} \effR \effL F_1
    }_\texttt{actual history} \seqA \underbrace{ \lastA
    \msg{op}{\phi'} }_\texttt{actual current } \seqA \underbrace{
    F_2 \effR }_\texttt{actual prophecy}
  \end{align*}} %
\noindent This is reflected in how the two are merged (line $4$ -
$6$). Finally, $\eff{op_{parent}}$ and $\eff{op}$ are added to the
forward and backward sets (line $8$).

\begin{example} We demonstrate the first step of how \ref{eqn:autoEx}
  is refined into \ref{eqn:autoEx2}. The refinement loop begins in the
  following state: %
  { \footnotesize
    \begin{align*}
      \Gamma \equiv\;& \Code{x}\hasoty{int}{\top},
                       \Code{y}\hasoty{int}{\top} \quad \Mfw \equiv \emptyset \quad
                       \Mbw \equiv \emptyset \\
      \Pi \equiv\;& \allTL \seqA \lastA
                    \msg{writeRsp}{v = \Code{x}} \seqA \globalA \neg
                    \msg{writeRsp}{\top} \seqA \lastA\msg{readRsp}{v = \Code{y}
                    \land st = \true \land \Code{y} \not= \Code{x}} \seqA \allTL
                    \tag{before iteration $1$}
    \end{align*}}%
  \noindent The first iteration targets the $\eff{readRsp}$ operation.
  Since $\Mfw$ is empty, the algorithm performs forward synthesis on
  $\eff{readRsp}$. No additional events are generated by the handler
  of $\eff{readRsp}$, so no events are added to the abstract
  trace. Since $\eff{readRsp}$ is not generable, the algorithm next
  performs backward synthesis. There the $\eff{readReq}$ in $\Delta$ have obligation contains $\eff{readRsp}$, which has an intersection \PAT, $\tau_1 \interty \tau_2$:%
  {\footnotesize
    \begin{align*}
      &\Code{x}\hasnty{int}\garr \rg{\finalA
        \msg{writeReq}{v = \Code{x}} \land
        \neg\nextA\finalA\msg{writeReq}{\top}}{\lastA\msg{readReq}{\top}}{\finalA\msg{readRsp}{v
        = \Code{x} \land st = \true}} \tag{$\tau_1$} \\
      & \rg{\neg\finalA
        \msg{writeRsp}{\top}}{\lastA\msg{readReq}{\top}}{\finalA\msg{readRsp}{st
        = \false}} \tag{$\tau_2$}
    \end{align*}}%
  \noindent The prophecy automaton of $\tau_2$ requires
  $\eff{readRsp}$ to have a \textsf{false} status, which is at odds
  with the current abstract trace. This inconsistency is detected by the non-emptiness check, causing our algorithm to backtrack and attempt to select another intersected type. Alternatively,
  $\tau_1$ can be merged with the current trace, resulting in the
  following updated values of the loop's variables:%
  { \footnotesize
    \begin{align*}
      \Gamma \equiv\;& \Code{x}\hasoty{int}{\top},
                       \Code{y}\hasoty{int}{\top} \quad \Mfw \equiv
                       \{\eff{readRsp}, \eff{readReq}\} \quad \Mbw
                       \equiv \{\eff{readRsp}\} \\
      \Pi \equiv\;& \allTL \seqA \lastA\msg{writeReq}{v =\Code{y}}
                    \seqA \globalA \neg \msg{writeReq}{\top} \seqA
                    \lastA\msg{readReq}{v = \Code{y}} \seqA \allTL
                    \seqA \lastA\msg{writeRsp}{v = \Code{x}} \seqA \\
                     & \globalA \neg \msg{writeRsp}{\top} \seqA \lastA\msg{readRsp}{v = \Code{y} \land st = \true \land \Code{y} \not= \Code{x}} \seqA \allTL \tag{after iteration $1$}
    \end{align*}} %
  \noindent
  The refined trace now includes events for $\eff{writeReq}$ and
  $\eff{readReq}$, and the values of the forward and backwards sets
  enable both events to be targeted by the next iteration of the loop.
  The remainder of the refinement steps used to construct a violating
  trace can be found in \techreport{}.
\end{example}

\begin{theorem}[Synthesis is Sound]
  \label{theorem:algo-sound}
  The controller synthesized by the algorithm is type-safe with
  respect to our declarative typing rules.
\end{theorem}
